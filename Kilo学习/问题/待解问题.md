# 未决问题

## 当前待验证

- `packages/opencode/src/session/` 中消息流转、压缩、摘要、重试的真实边界分别是什么
- `packages/opencode/src/provider/` 中 Kilo 自有网关与通用 AI SDK provider 的耦合点在哪里
- `packages/opencode/src/worktree/` 在 CLI 内核中的真实使用路径是否集中，还是分散在多个 route / command
- `packages/kilo-vscode/` 中 Agent Manager 对 worktree 和 session 的组织方式，哪些是扩展侧能力，哪些依赖 CLI
- `PermissionNext` 是否需要补 plugin hook 或其他策略注入点，才能更好支持企业级统一审批
- `agent`、`config`、organization modes 三者如何合并成最终执行策略
- 扩展 server route 之后，SDK regen 与客户端消费路径的最小闭环应该怎么走

## 后续处理方式

- 每解决一个问题，就将其迁移到对应的架构文档
- 如果发现原有结论不准确，优先修正文档而不是追加相互矛盾的说明
