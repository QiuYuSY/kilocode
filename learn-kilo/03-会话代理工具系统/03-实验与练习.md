# 03 实验与练习

## 第一阶段: 不看源码完成主任务

## 任务 1: 画一次工具调用生命周期

不看源码，先按你对课程讲义的理解画出这条线:

- `tool-input-start`
- `tool-call`
- `tool-result`
- `step-finish`

输出:

- 一张生命周期图
- 每个阶段写清会更新哪个 Part / Message / Session 状态

## 任务 2: 对比 4 个 Agent

不看源码，先根据课程讲义对比下面 4 个:

- `code`
- `plan`
- `debug`
- `ask`

输出表格:

- 它们的目标
- 它们的 permission 差异
- 它们为什么不能互相替代

## 任务 3: 用自己的话解释 Permission

不看源码，先回答:

- 为什么 Permission 属于运行时，而不是 UI 弹窗
- 它是怎么“卡住并恢复执行流”的

## 第二阶段: 回到源码做验证

## 验证 1: 追一次 Permission 执行流

只追下面几个点:

- `PermissionNext.ask(...)`
- pending request 存哪里
- 被 reply 之后如何继续

## 验证 2: 对照 Agent 定义修正你的对比表

再去 `agent.ts` 里核对你前面写的 4 个 Agent 对比，检查你误判的地方。

## 二开思考练习

回答下面 2 个问题:

1. 如果你想新增一个工具，最少要考虑哪些模块
2. 如果你想新增一个“只读分析型 Agent”，最少要考虑哪些行为边界

## 输出练习

写一段 300 字以内的说明:

“为什么 KiloCode 的会话系统本质上是执行系统，而不是聊天系统。”

## 完成标准

- 你画出了工具调用生命周期
- 你完成了 4 个 Agent 的对比
- 你已经能不看源码解释 Permission 的运行时位置
- 你能清楚解释 Permission 在运行时里的位置
