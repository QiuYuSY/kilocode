# 实验与练习

## 实验 1：只读 `Project.fromDirectory(...)`

打开：

- [packages/opencode/src/project/project.ts](/d:/Code/kilocode/packages/opencode/src/project/project.ts)

第一轮只看一个函数：

- `fromDirectory(...)`

不要看别的。

请按下面顺序记录：

1. 它怎样找到 `.git`
2. 它怎样处理 Git root / common-dir
3. 它怎样得到 project id
4. 它怎样把结果落到数据库

然后写一句话：

- “为什么 project id 不能只等于当前路径？”

## 实验 2：只读 `Instance.provide(...)` 和 `Instance.state(...)`

打开：

- [packages/opencode/src/project/instance.ts](/d:/Code/kilocode/packages/opencode/src/project/instance.ts)

第一轮请只盯这两个点：

- `provide(...)`
- `state(...)`

记录：

- `provide` 到底建立了什么
- `state` 到底按什么维度隔离
- 它和“模块内缓存变量”最大的区别是什么

## 实验 3：画出上下文关系

请把下面这些对象画在一张图里：

- request directory
- project
- sandbox
- worktree
- Instance

要求：

- 图上必须出现“谁依赖谁”
- 图上必须说明哪一层更接近“身份识别”，哪一层更接近“状态承载”

## 实验 4：只追配置主干

打开：

- [packages/opencode/src/config/config.ts](/d:/Code/kilocode/packages/opencode/src/config/config.ts)

第一轮只追这些名字：

- `stateInit`
- `state`
- `get`
- `getGlobal`
- `global`

不要试图读完整个文件，也不要追所有 schema。

你只需要做一件事：

把你看到的配置来源按顺序抄出来。

## 实验 5：看数据库入口

打开：

- [packages/opencode/src/storage/db.ts](/d:/Code/kilocode/packages/opencode/src/storage/db.ts)

这轮只看：

- `Database.Path`
- `Client`
- `use`
- `transaction`

然后回答：

- 数据库层在给上层提供什么统一能力？

## 练习 1：画两张图

### 图 1

Project / Instance / directory / worktree 关系图

### 图 2

配置来源层次图

## 练习 2：说明题

请写 400 到 600 字：

“为什么这个系统必须按目录隔离状态？”

要求必须提到：

- 多目录
- worktree
- 配置作用域
- 长生命周期服务进程

## 练习 3：脱稿解释

请尝试口头讲 3 分钟：

“为什么真实工程里的配置和存储不会像小项目那样简单？”

## 实验记录模板

你可以按这个结构写：

- `Project.fromDirectory(...)` 我看到了什么
- `Instance.provide(...)` / `state(...)` 我看到了什么
- 配置来源有哪些
- 数据库存储做什么
- 文件存储为什么还需要存在
