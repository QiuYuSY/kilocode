# 04 实验与练习

## 第一阶段: 不看源码完成主任务

## 任务 1: 画扩展接入链

不看源码，先按课程讲义画出三层:

- extension host
- CLI backend 连接层
- webview UI

输出:

- 一张“扩展接入链图”
- 每一层写一句职责说明
- 一句总结: 为什么扩展不是系统主体

## 任务 2: 拆共享连接的必要性

不看源码，先回答:

- 为什么多个 webview 不能各自维护一套后端连接
- 如果不共享连接，会出现哪 3 类问题

## 第二阶段: 回到源码做验证

## 验证 1: 只看 `ServerManager`

请只回答 4 个问题:

1. CLI 是怎么找到的
2. 进程是怎么启动的
3. 端口是怎么发现的
4. 进程是怎么销毁的

## 验证 2: 只看 `KiloConnectionService`

请只回答:

- 为什么它要持有共享 client
- 为什么它要广播事件
- 为什么它要处理 pending permission / question 的 drain

## 输出练习

写一段说明:

“为什么 Agent Manager 是共享后端上的多会话编排，而不是另一套后端系统。”

## 完成标准

- 你已经画出扩展接入链
- 你已经能在不看源码时说明为什么连接必须共享
- 你已经能解释共享连接存在的原因
- 你已经知道扩展改动应该切在哪一层
