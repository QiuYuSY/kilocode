# 04 课后总复盘模板

## 本课三层结构

- extension host:
- CLI backend 连接层:
- webview UI:

## 第一轮: 不看源码时我已经能讲什么

- 我能先讲出的扩展三层职责:
- 我能先解释的共享连接必要性:

## 我确认下来的关键文件

- `extension.ts` 负责:
- `ServerManager` 负责:
- `KiloConnectionService` 负责:

## 我最重要的设计判断

- 为什么连接必须共享:
- 为什么 Agent Manager 不是另一套后端:

## 第二轮: 源码验证后我修正了什么

- 我原来对 `ServerManager` 的误解:
- 我原来对 `KiloConnectionService` 的误解:

## 我已经能解释的链路

- 扩展如何拉起 `kilo serve`:
- 事件如何从后端走到多个视图:

## 后续二开落点

- 如果我要加一个扩展命令，我先看:
- 如果我要改会话同步，我先看:
