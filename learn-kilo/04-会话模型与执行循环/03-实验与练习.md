# 03-实验与练习

## 实验 1：只看 Session 顶层，不看执行

打开：

- [packages/opencode/src/session/index.ts](/d:/Code/kilocode/packages/opencode/src/session/index.ts)

第一轮只做这件事：

找到 `Session.Info`，然后回答：

- 它有哪些字段一眼看出它不是普通聊天记录？
- 哪些字段明显和执行系统有关？
- 哪些字段明显和项目上下文有关？

要求：

- 不要一开始就追完整个文件
- 只围绕 `Info` 和 `create` 附近阅读

## 实验 2：只看 Message 与 Part

打开：

- [packages/opencode/src/session/message-v2.ts](/d:/Code/kilocode/packages/opencode/src/session/message-v2.ts)

这轮不要试图读完所有类型。

请只抓这些类型：

- `TextPart`
- `ToolPart`
- `StepStartPart`
- `StepFinishPart`
- `PatchPart`
- `ReasoningPart`

然后回答：

1. 这些 Part 各自解决了什么表达问题？
2. 如果没有这些 Part，Message 还能不能完整表达执行过程？

## 实验 3：画出 Session 数据模型图

请把下面这些对象画在一张图里：

- Session
- parent / child session
- Message
- text part
- tool part
- patch part
- reasoning part

要求：

- 图中要标出“谁包含谁”
- 图中要标出哪些更接近“对话”、哪些更接近“执行”

## 实验 4：只追一次工具调用生命周期

打开：

- [packages/opencode/src/session/processor.ts](/d:/Code/kilocode/packages/opencode/src/session/processor.ts)

这一次不要读完整个文件。

只追下面这些分支：

- `tool-input-start`
- `tool-call`
- `tool-result`
- `tool-error`

请把你看到的状态变化按顺序写下来。

建议你至少回答：

1. tool 从什么时候开始有自己的 `Part`
2. pending / running / completed / error 分别出现在哪里
3. 最终是如何写回会话系统的

## 实验 5：只看 `finish-step`

仍然在：

- [packages/opencode/src/session/processor.ts](/d:/Code/kilocode/packages/opencode/src/session/processor.ts)

这一轮只看：

- `finish-step`

请记录：

- 它在做哪些“收尾和结算”动作
- 哪些动作是对 Message 的更新
- 哪些动作是对 Part 的更新

## 练习 1：画执行时间线

请画一条最小执行时间线，至少包括：

- `start-step`
- 文本流
- 工具流
- `finish-step`

## 练习 2：说明题

请写 400 到 600 字，回答：

“为什么 Message 必须拆成 Part？”

要求必须提到：

- 流式输出
- 工具状态
- patch
- UI 过程展示

## 练习 3：脱稿复述

请尝试口头讲 3 分钟：

“为什么 `SessionProcessor` 不适合被设计成一个普通同步函数？”

## 实验记录模板

你可以按下面结构写：

- Session 顶层字段给我的感觉
- Message 和 Part 是怎么分层的
- tool call 生命周期
- `finish-step` 在做什么
- 我现在如何理解“会话系统”
