# 03-实验与练习

## 实验 1：只对比 4 个核心模式

打开：

- [packages/opencode/src/agent/agent.ts](/d:/Code/kilocode/packages/opencode/src/agent/agent.ts)

第一轮不要试图理解全部模式。

请只对比：

- `code`
- `plan`
- `ask`
- `debug`

记录：

1. 它们的主要用途是什么
2. 哪些更偏执行，哪些更偏解释
3. 哪些 permission 风格明显不同

## 实验 2：只读 permission 主干

打开：

- [packages/opencode/src/permission/next.ts](/d:/Code/kilocode/packages/opencode/src/permission/next.ts)

第一轮只盯下面几个名字：

- `fromConfig`
- `merge`
- `ask`
- `reply`
- `evaluate`

不要试图吃透每个边缘规则。

请回答：

1. permission 规则是怎么从配置变成运行时规则集的？
2. permission 请求是在什么时候被真正判断的？
3. `ask` / `reply` 说明它为什么不只是静态配置？

## 实验 3：只看 ToolRegistry 主干

打开：

- [packages/opencode/src/tool/registry.ts](/d:/Code/kilocode/packages/opencode/src/tool/registry.ts)

这轮只盯：

- `state`
- `all()`
- `tools(...)`
- `fromPlugin(...)`

请记录：

1. 默认内置工具从哪里来
2. 条件工具从哪里来
3. 插件工具从哪里来
4. 模型如何影响最终工具集合

## 实验 4：选一个工具做最小追踪

推荐打开其中一个：

- [packages/opencode/src/tool/read.ts](/d:/Code/kilocode/packages/opencode/src/tool/read.ts)
- [packages/opencode/src/tool/bash.ts](/d:/Code/kilocode/packages/opencode/src/tool/bash.ts)
- [packages/opencode/src/tool/apply_patch.ts](/d:/Code/kilocode/packages/opencode/src/tool/apply_patch.ts)

请回答：

1. 参数是怎样定义的？
2. 执行入口在哪里？
3. 它最后如何被 ToolRegistry 收进系统？

## 练习 1：Agent 模式对比表

请做一张表，至少包含：

- 模式名
- 主要用途
- 是否更偏执行 / 解释
- 权限风格

## 练习 2：说明题

请写 400 到 600 字：

“为什么 Agent 模式的差异不只是 prompt？”

要求必须提到：

- permission
- 行为边界
- 可执行动作

## 练习 3：脱稿复述

请尝试口头讲 3 分钟：

“为什么 ToolRegistry 不能只是一个静态工具数组？”

## 实验记录模板

你可以按下面结构写：

- 我比较了哪些模式
- 我看到的 permission 主干
- ToolRegistry 是怎么汇总工具的
- 我追踪了哪个工具
- 我现在如何理解模式、权限和工具的关系
