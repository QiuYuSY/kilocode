# 03-实验与练习

## 实验 1：先把 `extension.ts` 当“装配图”看

打开：

- [packages/kilo-vscode/src/extension.ts](/d:/Code/kilocode/packages/kilo-vscode/src/extension.ts)

这一次不要逐行硬啃。

请只记录下面 4 类东西：

1. 注册了哪些 provider
2. 初始化了哪些 shared service
3. 哪些地方会把 webview 和后端连接起来
4. 哪些地方和 Agent Manager 直接相关

要求：

- 每类至少写 2 个观察点

## 实验 2：只追 `ServerManager`

打开：

- [packages/kilo-vscode/src/services/cli-backend/server-manager.ts](/d:/Code/kilocode/packages/kilo-vscode/src/services/cli-backend/server-manager.ts)

只回答下面 4 个问题：

1. 它怎样找到 CLI 可执行文件？
2. 它怎样启动 `kilo serve --port 0`？
3. 它怎样知道服务启动成功？
4. 它怎样做超时和销毁处理？

## 实验 3：只追 `KiloConnectionService`

打开：

- [packages/kilo-vscode/src/services/cli-backend/connection-service.ts](/d:/Code/kilocode/packages/kilo-vscode/src/services/cli-backend/connection-service.ts)

这一轮不要追所有方法。

只看：

- `connect`
- `doConnect`
- `onEvent`
- `onStateChange`

记录：

1. 为什么这里必须有共享连接服务？
2. 它为什么同时管 SDK 和 SSE？
3. 它为什么还要关心 health poll？

## 实验 4：只看 webview 承接层

打开：

- [packages/kilo-vscode/webview-ui/src/App.tsx](/d:/Code/kilocode/packages/kilo-vscode/webview-ui/src/App.tsx)

不要深挖 UI 组件细节。

只回答：

1. 它通过哪些 provider 接住上层能力？
2. 它更像“业务执行层”还是“展示交互层”？
3. 它和 extension host 的边界在哪里？

## 实验 5：如果你愿意，实际跑一次扩展

执行：

```powershell
bun run extension
```

请记录：

- 你预期扩展启动后会先发生什么
- 实际看到的行为和你的预期是否一致
- 如果“连不上后端”，你第一反应会看哪个文件

## 练习 1：画三层架构图

请画一张图，至少包含：

- extension host
- webview
- CLI backend

要求：

- 图中必须标出“谁负责系统能力，谁负责 UI，谁负责执行核心”

## 练习 2：画连接链路图

请画一张图，至少包含：

- `extension.ts`
- `ServerManager`
- `KiloConnectionService`
- SDK
- SSE
- webview

## 练习 3：脱稿复述

请尝试口头讲 3 分钟：

“为什么 Agent Manager 可以支持多 session，但仍然不需要多个后端？”

## 实验记录模板

你可以按下面结构写：

- 我在 `extension.ts` 里看到的装配角色
- `ServerManager` 在做什么
- `KiloConnectionService` 在做什么
- webview 处于哪一层
- 我现在如何理解 Agent Manager
