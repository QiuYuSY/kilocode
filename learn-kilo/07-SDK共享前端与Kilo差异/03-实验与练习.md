# 03-实验与练习

## 实验 1：只读 SDK 入口

打开：

- [packages/sdk/js/src/client.ts](/d:/Code/kilocode/packages/sdk/js/src/client.ts)
- [packages/sdk/js/src/index.ts](/d:/Code/kilocode/packages/sdk/js/src/index.ts)

第一轮只回答：

1. `createKiloClient` 做了哪些关键动作？
2. 它为什么不只是“new 一个 client”？
3. 它为什么适合成为多个客户端共享的桥梁？

## 实验 2：只看共享前端入口

打开：

- [packages/app/src/entry.tsx](/d:/Code/kilocode/packages/app/src/entry.tsx)

请记录：

1. app 入口在做什么
2. 它是怎样接服务器地址的
3. 它更像“产品应用入口”还是“底层执行层”？

## 实验 3：只看组件层边界

打开：

- [packages/kilo-ui/package.json](/d:/Code/kilocode/packages/kilo-ui/package.json)

不要去看全部组件源码。

只回答：

1. 这个包导出了哪些类型的能力？
2. 它为什么不只是样式资源？
3. 它为什么适合被多个客户端复用？

## 实验 4：只看 Kilo 增量入口

打开：

- [packages/kilo-gateway/src/index.ts](/d:/Code/kilocode/packages/kilo-gateway/src/index.ts)
- [packages/kilo-telemetry/src/index.ts](/d:/Code/kilocode/packages/kilo-telemetry/src/index.ts)
- [packages/opencode/src/kilocode](/d:/Code/kilocode/packages/opencode/src/kilocode)

请记录：

1. 哪些能力明显不是 upstream 通用核心
2. 哪些更像平台层能力
3. 为什么它们要尽量和通用核心保持边界

## 练习 1：画三层关系图

请画一张图，至少包含：

- server
- sdk
- client

要求：

- 必须标出契约层位置
- 必须说明客户端并不是直接裸连所有后端细节

## 练习 2：画共享前端关系图

请画一张图，至少包含：

- `packages/app`
- `packages/kilo-vscode/webview-ui`
- `packages/kilo-ui`

要求：

- 图中必须说明“共享了什么”和“没有完全共享什么”

## 练习 3：脱稿复述

请尝试口头讲 3 分钟：

“为什么 SDK、共享前端层和 Kilo 增量层都不是边角料，而是大型工程里必然会出现的桥梁层？”

## 实验记录模板

你可以按下面结构写：

- SDK 在做什么
- app 入口在做什么
- `kilo-ui` 为什么是基础设施
- 我看到的 Kilo 增量落点
- 我现在如何理解 bridge layer
