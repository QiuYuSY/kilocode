# 07 实验与练习

## 第一阶段: 不看源码完成主任务

## 任务 1: 给 4 类改动写验证清单

请分别给下面 4 类改动写最小验证清单:

1. 核心 tool 改动
2. 新增 route
3. 扩展端 UI 改动
4. Kilo telemetry 改动

## 任务 2: 写一份调试排查顺序

题目:

“扩展端连不上 CLI backend 时，你的排查顺序是什么？”

要求:

- 至少写 5 步
- 必须体现你对主链的理解

## 任务 3: 做一次交付风险清点

请列出你认为最容易遗漏的 6 个点，例如:

- SDK regen
- source links
- `kilocode_change`
- 路由改了但客户端没跟

## 第二阶段: 回到源码做验证

## 验证 1: 对照真实仓库命令修正你的验证清单

回到:

- 根 `AGENTS.md`
- `packages/opencode/AGENTS.md`

检查你的验证清单有没有漏掉:

- SDK regen
- source links
- `check-kilocode-change`

## 验证 2: 对照扩展主链检查排查顺序

再回到扩展相关文件，检查你的“扩展端连不上 backend”的排查顺序是否符合真实主链。

## 输出练习

写一份一页纸模板:

“我以后每次提交 KiloCode 改动前要走的交付检查表”

## 完成标准

- 你已经会为不同类型改动写不同的验证清单
- 你已经能先写排查顺序，再用仓库真实命令和主链校验
- 你已经有了稳定的调试排查顺序
- 你已经建立基础交付检查表
