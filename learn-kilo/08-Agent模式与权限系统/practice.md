# 练习

## 入门练习

对比：

- `code`
- `plan`
- `ask`
- `debug`

## 追链练习

说明一个 agent 的 permission 是怎样组合出来的。

## 口头复述任务

脱稿解释：

“为什么 `ask` 模式不能和 `code` 模式使用同一套默认权限？”

## 输出练习

写一段解释：

为什么 permission 不是 UI 弹窗逻辑，而是运行时安全边界。

## 参考结构

- 模式定义在哪里
- permission 怎么合成
- `code` 与 `ask` 差在哪
- 为什么这不是 UI 细节
