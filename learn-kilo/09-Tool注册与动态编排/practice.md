# 练习

## 入门练习

列出 `ToolRegistry` 默认注册的主要工具类别。

## 追链练习

任选一个工具，说明它如何进入 `ToolRegistry` 并暴露给模型。

## 真实工程实验题

做一次“工具注册清点实验”：

- 打开 `packages/opencode/src/tool/registry.ts`
- 只看 `all()`
- 记录你看到的默认工具、条件工具、插件工具三类来源

## 口头复述任务

脱稿解释：

“为什么工具系统不能只是一个写死的数组？”

## 输出练习

解释：

为什么 `apply_patch` 和 `edit/write` 不能简单同时全部暴露。

## 参考结构

- 工具从哪里来
- 谁决定是否启用
- 模型怎么影响工具集
- 插件怎么插进来
